import React from 'react';
import './button.css';

export default function Button(props) {
  return (

    //   <div className="genButton" onClick={props.onClick}>{props.children}</div>
        <input type='submit' className="genButton"/>
      );

    }

    
    
    
    
    
    
    
    
    
